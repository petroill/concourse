---
platform: linux

image_resource:
  type: registry-image
  source: {repository: busybox}

inputs:
  - name: scripts

run:
  path: /bin/sh
  args:
    - -c
    - |
      ls -la
      ./scripts/task_show_uname.sh
